apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: katehaki
spec:
  type: ClusterIP
  selector:
    app: alertmanager
  ports:
  - name: alertmanager
    port: 9093
    targetPort: alertmanager
